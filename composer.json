{
	"name": "germania-kg/routenameurlcallable",
	"description": "Generates full URL for Slim 4's named routes, using RouteContext und RouteParser.",

	"keywords": [ "named routes", "routes", "callable", "slim", "slim4", "UriInterface", "RouteContext", "RouteParser"],

	"license": "MIT",

	"authors": [
		{
			"name":  "Carsten Witt",
			"email": "admin@germania-kg.com",
			"role":  "Developer"
		}
	],

	"autoload": {
		"psr-4": {
			"Germania\\RouteNameUrlCallable\\": "src"
		}
	},

  "autoload-dev": {
      "psr-4": {
      	"tests\\": "tests/src"
      }
  },

	"require": {
		"php": "^7.1",
		"slim/slim": "^4.0"
	},

    "require-dev": {
        "php-coveralls/php-coveralls": "^2.0",
        "phpunit/phpunit": "^7.0|^8.0",
		"slim/psr7": "^0.6.0"
    },

    "scripts": {
          "phpunit": "phpunit --colors=auto --coverage-text",

          "test": [
              "@phpunit"
          ]
    }

}
